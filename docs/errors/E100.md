# E100 — config error (generic)

The configuration is invalid in some way. This is the parent code for
`E101` (cannot parse) and `E102` (parses but breaks cross-field
rules). Look at the more specific code in your error message for the
actionable case.
